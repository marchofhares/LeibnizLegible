# Leibniz Legible — https://github.com/marchofhares/leibnizlegible
#
# The human pages are open to crawlers. The JSON API, the IIIF manifests and
# the annotation pages are for viewers and scripts that ask for them, not for
# blind crawling: a crawler walking /manifests/ would request the page image
# of every canvas of every work (from the GWLB, or from the mirror) for nothing.
User-agent: *
Disallow: /api/
Disallow: /manifests/
Disallow: /annotations/
Disallow: /search?
Crawl-delay: 5
