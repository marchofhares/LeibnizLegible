# /etc/leibniz-legible/caddy.env — read by Caddy through the systemd drop-in
# install.sh writes (/etc/systemd/system/caddy.service.d/leibniz.conf).
# The Caddyfile's {$LEIBNIZ_DOMAIN} and {$LEIBNIZ_UPSTREAM} come from here.
LEIBNIZ_DOMAIN=leibnizlegible.com
LEIBNIZ_UPSTREAM=127.0.0.1:8000
